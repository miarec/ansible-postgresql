---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ansible-postgresql-${MOLECULE_DISTRO:-ubuntu2404}
    image: ghcr.io/miarec/${MOLECULE_DISTRO:-ubuntu2404}-systemd:latest
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    privileged: true
    pre_build_image: true

provisioner:
  name: ansible
  playbooks:
    converge: ${MOLECULE_PLAYBOOK:-converge.yml}
  env:
    # As of Jan 2026, PGDG repo for RHEL8 only serves PostgreSQL 13-16.
    # See https://download.postgresql.org/pub/repos/yum/
    POSTGRESQL_VERSION: ${MOLECULE_POSTGRESQL_VERSION:-15}
    ANSIBLE_VERBOSITY: ${MOLECULE_ANSIBLE_VERBOSITY:-0}
    ANSIBLE_ROLES_PATH: ${MOLECULE_PROJECT_DIRECTORY}/../
verifier:
  name: testinfra
  options:
    s: true
    verbose: true
