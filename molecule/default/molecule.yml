---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ansible-postgresql-${MOLECULE_DISTRO:-ubuntu2404}
    image: ghcr.io/miarec/${MOLECULE_DISTRO:-ubuntu2404}-systemd:latest
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    privileged: true
    pre_build_image: true

provisioner:
  name: ansible
  playbooks:
    converge: ${MOLECULE_PLAYBOOK:-converge.yml}
  env:
    # As of May 2026, PGDG repo for RHEL8+ serves PostgreSQL 12-17.
    # See https://download.postgresql.org/pub/repos/yum/
    # Accepts: 12, 13, 14, 15, 16, 17
    POSTGRESQL_VERSION: ${MOLECULE_POSTGRESQL_VERSION:-17}
    ANSIBLE_VERBOSITY: ${MOLECULE_ANSIBLE_VERBOSITY:-0}
    ANSIBLE_ROLES_PATH: ${MOLECULE_PROJECT_DIRECTORY}/../
verifier:
  name: testinfra
  options:
    s: true
    verbose: true
